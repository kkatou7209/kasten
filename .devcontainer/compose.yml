name: casten

services:
  bun:
    build: .
    volumes:
      - ..:/workspace
      - kasten_node_modules:/workspace/node_modules
    tty: true
    command: sleep infinity
    
volumes:
  kasten_node_modules: